Max Totten, mastotte, pa3

List.h:
    Defines the functions that are used in List.c.
    #included directly in Graph.h, List.c
    Should not be called.
List.c
    List manipulation functions, from pa1.
    Should not be called.
Graph.c
    Graph manipulation functions, used by FindComponents.c and GraphClient.c.
    Should not be called.
GraphClient.c
    Tests the functions in Graph.c.
    How to call:
        make
        make GraphClient
        ./GraphClient
FindComponents.c:
    How to call: 
        make
        make FindPath
        ./FindPath <inputfile> <outputfile>
    Finds the strongly connected components based on
    the graph read from inputfile.
    Outputs analysis of graph to outputfile.
Makefile
    Builds the program.
